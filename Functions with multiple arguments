import turtle
import time

my_turtle = turtle.Turtle()

def square(length):

    my_turtle.forward(length)
    my_turtle.left(90)
    my_turtle.forward(length)
    my_turtle.left(90)
    my_turtle.forward(length)
    my_turtle.left(90)
    my_turtle.forward(n)

print(square(200)) # basically we input the length of the square here in this line of code

time.sleep(2)
